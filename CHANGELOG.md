# 更新日志 (Changelog)

本文档记录 **Just Translate**（智能翻译、润色与词典工作台）自立项以来的全版本迭代历史、架构演进与技术变更记录。

本规范严格遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式，并采用 [语义化版本 (Semantic Versioning)](https://semver.org/lang/zh-CN/) 规范。

---

## [1.0.0] - 2026-09-16 (首个正式发布版)

### 🌟 核心功能与亮点 (Core Features)
- **三大生产力工作区**：集智能多模态翻译、母语级文字润色与现代卡片双语词典于一体。
- **现代化结构化卡片词典**：
  - 统一 7 标签黄金协议骨架（`[WORD]`、`[PRON]`、`[DEFS]`、`[EXAMPLES]`、`[PHRASES]`、`[SYNONYMS]`、`[ANTONYMS]`），正面引导输出多词性释义与 2~3 条典型双语例句；
  - 词头防污染防护、词性白名单校验、双语对照释义提取与自由格式深度解析；
  - 词头与例句独立发音朗读（🔊）与一键复制（📋），配备 Windows 剪贴板并发占锁防护。
- **模式化交互与状态联动**：
  - 输入面板执行主按钮根据模式动态切换（`🚀 翻译` / `✨ 润色` / `🔎 查询` / `🔍 识别并处理`）；
  - 原文在请求后被修改时，醒目呈现常驻琥珀色小标签（`⚠ 原文已修改，结果可能不准确`）。
- **本地回环网络智能直连**：
  - 底层网络调度自动对 `127.0.0.1`、`localhost` 绕过系统 `HTTP_PROXY` / `HTTPS_PROXY` 代理，保障本地部署大模型（端口 8001 / 11434 等）秒级稳定直连。
- **可靠任务并发与生命周期控制**：
  - 模式级自增 `task_id`，新任务发起时自动安全撤销旧任务；
  - 实时物理断开活跃底层 Socket 连接，停止按钮与新任务打断立即生效，无界面阻塞。
- **高保真语音朗读体系 (TTS)**：
  - 深度集成微软 Edge Neural 在线高保真神经语音引擎；
  - 支持无外网时自动降级调用本地 Windows SAPI5 语音，提供完善的语音包检测与安装指引。
- **多模态视觉图片 OCR 识别**：
  - 支持截图快捷粘贴（`Ctrl+V`）与图片拖拽载入，集成 GLM-OCR / OpenAI Vision 实现秒级文字提取并自动触发处理流水线。
- **逐句双语校对与单句独立重译**：
  - 智能多语种句子边界切分，悬停原文时目标译文对应句子同步高亮；
  - 支持在译文面板右键呼出菜单对疑义单句进行原地重新翻译。
- **自研防塌陷平衡分栏器 (`BalancedSplitter`)**：
  - 设定 280px 最小安全宽度，支持双击中央分隔条 1:1 对等复位。
- **视觉主题与「关于」界面**：
  - 具备暗黑极客 (Dark) 与明亮纯净 (Light) 双套高质感 QSS 主题；
  - 全局设置对话框新增「关于」选项卡（明确标注软件作者为 **Kutori**，当前版本号为 **1.0.0**，采用 MIT 开源协议）。
- **本地持久化与数据隐私**：
  - 本地 SQLite 历史记忆数据库（`history_manager.py`），提供 `Ctrl+H` 模糊检索，完全无云端泄露风险；
  - 严格设定 30 秒网络超时限制与自动重试机制。
- **质量保证**：
  - 全量自动化单元测试达到 **99 项用例**，测试通过率保持 **100% (99/99 OK)**。

---

## 发布前测试版历史 (Pre-release Beta Versions)

### [1.6.2-beta] - 2026-09-16 (发布前测试版)
- **词典提示词架构深度融合与去噪优化**：
  - 吸收省钱模式 7 标签清晰骨架，彻底移除正常模式中堆砌的负向惩罚词汇（“绝对严禁”、“最重要铁律”等），解决小参数模型（如 `Hy-MT2`）过度约束导致提前截断 `<eos>` 丢失例句的问题；
  - 在模板中显式注入待查词原形（`[WORD] {clean_word}`），杜绝模型将长句或释义错写在词头行；
  - 正常模式正面引导 2~4 条多词性释义与 2~3 条自然地道的双语典型例句；
  - 增加 Windows 剪贴板并发占锁防护重试机制。

### [1.6.1-beta] - 2026-09-16 (发布前测试版)
- **词典正常模式与省钱模式缺陷修复与解析健壮性增强**：
  - 移除了先前在双语提示词中误加的全局“严禁使用任何英文单词或英文释义”硬性禁令，修正英英及同语言查询下的双语对照逻辑；
  - 词头内容污染三层防御（Prompt 约束、解析器 `|` 剥离回补 definitions、UI 卡片 `_query_word` 兜底）；
  - 解析器自由格式深度提取与 `VALID_POS_TAGS` 词性白名单；
  - 流式降级纯文本卡片防闪烁保护。

### [1.6.0-beta] - 2026-09-16 (发布前测试版)
- **模式化主按钮文案与状态联动**（`🚀 翻译` / `✨ 润色` / `🔎 查询` / `🔍 识别并处理`）；
- **原文修改后结果过期琥珀色小标签预警**（`⚠ 原文已修改，结果可能不准确`）；
- **新增全局设置「关于」选项卡**（作者：**Kutori**，版本号：**1.0.0**）；
- **本地 Loopback 模型环境代理智能绕过**（`127.0.0.1` 直连）；
- **任务并发撤销与实时停止生命周期可靠性治理**（严格自增 `task_id` 与 `StreamController` 物理断开）。

### [1.5.0-beta] - 2026-09-16 (发布前测试版)
- **词典核心释义双语严格对照**与源目标语言动态匹配；
- **词典发音语言精准定向路由**；
- **典型双语例句独立发音与复制按钮增强**；
- **润色流式防闪烁重绘消除**；
- **词典面板响应式排版优化与过时快捷键清理**。

### [1.4.0-beta] - 2026-09-15 (发布前测试版)
- **现代结构化卡片词典面板 (`DictionaryCardPanel`) 初步引入**；
- **自适应流式布局 (`FlowLayout`)**；
- **词典渐进式流式解析器 (`dictionary_parser.py`)**。

### [1.3.0-beta] - 2026-09-15 (发布前测试版)
- **微软 Edge Neural TTS 高保真神经语音引擎**；
- **Windows 本地 SAPI5 离线降级与语音包检测指引**；
- **多媒体异步非阻塞播放流水线**。

### [1.2.0-beta] - 2026-09-15 (发布前测试版)
- **自主研发防塌陷平衡分栏器 (`BalancedSplitter`)**（280px 最小安全宽度、双击中线 1:1 对等复位）。

### [1.1.0-beta] - 2026-09-14 (发布前测试版)
- **逐句智能对齐与悬浮对照高亮引擎 (`SentenceAligner`)**；
- **单句独立右键重译机制**；
- **GLM-OCR 视觉图片识别与两阶段翻译流水线**。

### [0.1.0 - 0.9.0-beta] - 2026-09-12 ~ 2026-09-13 (原型孵化与早期基线)
- 项目技术选型与 PySide6 基础窗口骨架；
- 原生 SSE 流式打字机交互引擎；
- 暗黑与明亮双套主题支持及 32 位 RGBA 透明图标规范；
- 本地 SQLite 历史持久化存储与 Eco 极简模式。
