# 更新日志 (Changelog)

本文档记录 **Just Translate**（智能翻译、润色与词典工作台）自立项以来的全版本迭代历史、架构演进与技术变更记录。

本规范严格遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式，并采用 [语义化版本 (Semantic Versioning)](https://semver.org/lang/zh-CN/) 规范。

---

## [1.6.0] - 2026-09-16 (当前最新稳定版)

### 🌟 重点特性与突破 (Major Highlights)
- **模式化主按钮文案与状态联动**：
  - 翻译模式明确展示 `🚀 翻译`；
  - 润色模式明确展示 `✨ 润色`；
  - 词典模式明确展示 `🔎 查询`；
  - 载入图片 OCR 识别时智能切换为 `🔍 识别并处理 (Ctrl+Enter)`，清除图片后即刻恢复当前模式对应的专属文案；
  - 切换模式时按钮文本立即响应更新。
- **原文修改后结果过期琥珀色小标签预警**：
  - 在输出区域标题与状态旁新增醒目的琥珀色常驻小标签：`⚠ 原文已修改，结果可能不准确`；
  - 记录每次请求对应的原文快照；在生成中或完成后，只要用户修改输入框文本，标签即刻出现；
  - 若用户将文本改回快照或针对新原文重新发起请求，提示自动清除并保持隐藏；
  - 严格区分用户手动编辑与程序性 `set_text`（历史导入、回译、一键润色、OCR提取回填等），彻底杜绝无意义的误报。
- **新增「关于」信息选项卡 (About Tab)**：
  - 在全局设置对话框中新增独立选项卡【关于】；
  - 优雅呈现软件高清品牌图标、软件名称与定位标语；
  - 核心展示软件作者为 **Kutori**、版本号为 **1.0.0**；
  - 呈现技术架构栈、核心三大工作区模式与开源协议（MIT License），并在暗黑/明亮双主题下实现高品质自适应排版。
- **本地 Loopback 模型环境代理智能绕过**：
  - 新增底层网络路由调度模块 `core/network_utils.py`；
  - 对 `127.0.0.1`、`localhost`、`::1` 等本地回环地址，显式绕过系统的 `HTTP_PROXY` / `HTTPS_PROXY` 代理，彻底解决本地模型（llama.cpp server 8001、Ollama 11434 等）被代理错误转发而连接失败的问题；
  - 远程商业接口（DeepSeek、OpenAI 等）完全保留并继续尊重环境代理配置；统一覆盖模型探测（`/models`）、连通性测试、流式生成及 OCR 识图。
- **任务并发撤销与停止生命周期可靠性治理**：
  - 引入模式级严格自增 `task_id` 机制，彻底丢弃旧任务的一切残留信号（Token、完成、错误、状态、速度），消除旧任务污染新界面的风险；
  - 用户连续提交时执行“取消并替换旧任务”语义；
  - 移除主线程中阻塞界面的 `prev_worker.wait(500)`，杜绝 GUI 卡顿；
  - 引入 `StreamController` 强行快速断开底层活跃网络 Socket，停止按钮与新任务打断立即生效，无需死等下一个 Token；
  - 主动停止的任务状态清晰标记为“已停止”，保留已生成的部分内容供查看，不弹报错，且严禁写入正常完成历史；
  - 保留生成中可自由编辑原文的能力。

### 🧪 质量与测试验证
- 单元测试套件全量扩充至 **92 项用例**（新增设置关于选项卡作者与版本号校验、图标解析路由等专项测试），测试通过率保持 **100%（92/92 OK）**。

---

## [1.5.0] - 2026-09-16

### 🌟 重点特性与突破 (Major Highlights)
- **词典核心释义双语严格对照**：彻底重塑词典系统提示词与正则流式解析引擎，严格根据顶栏选定的源语言与目标语言进行双语对照呈现（例如：中文 ➔ 日文模式下，核心释义严格输出日文母语解释与中文对照解释，彻底杜绝混杂不相干英文的缺陷）。
- **词典发音语言精准定向路由**：修复了当词头含有日文汉字时被底层语音引擎误判为中文、从而发出中文语音的严重缺陷。现全面以顶栏当前目标语言为最高准则（中译日模式严格调用日语语音引擎与假名注音发音）。
- **典型双语例句全面增强**：
  - 为双语例句模块中的每一句配备独立发音朗读（🔊）与一键复制（📋）按钮；
  - 统一了词头与例句按钮的标准尺寸（24x24dp）及悬浮/按压微动效；
  - 修复了例句解析时因大模型输出不规范导致的标记错误及单语丢失问题。
- **润色流式防闪烁与阅读平滑保护**：借鉴翻译板块的高效阅读机制，重构了润色输出面板的生命周期。流式打字结束后直接保持纯文本排版与光标焦点，彻底消除 `setHtml` 强制重绘带来的 DOM 销毁、视口上下跳动与光标失焦。
- **词典面板响应式排版优化**：重构了词典滚动区域与卡片容器的宽度策略（`minimumWidth` 与 `sizePolicy` 弹性伸缩），确保在小窗口或窄分栏模式下所有操作按钮与双语文本完整自适应显示，不再被异常裁剪。
- **精简交互链路与快捷键清理**：彻底废弃并移除了输入面板中已过时的“同反义词 (Alt+S)”独立按钮与全局 `Alt+S` 快捷键，统一归拢于卡片词典全景流式展示之中。

### 🧪 质量与测试验证
- 单元测试套件全量扩充至 **68 项用例**（涵盖核心业务逻辑、流式防重绘、卡片词典生命周期、语言检测、TTS 语音路由、BalancedSplitter 防塌陷保护、GLM-OCR 识图等），测试通过率保持 **100%（68/68 OK）**。

---

## [1.4.0] - 2026-09-15

### ✨ 新增 (Added)
- **现代结构化卡片词典面板 (`DictionaryCardPanel`)**：
  - 告别传统大段原始 Markdown 的杂乱排版，将大模型查词结果动态转化为清晰美观的视觉卡片；
  - 包含词头展示区、国际音标/假名、双语核心释义、典型例句卡片、自适应流式短语及近反义词群。
- **自适应流式布局 (`FlowLayout`)**：
  - 针对词典近义词、反义词和短语标签，开发了原生 Qt 流式自适应折行布局组件，随窗口宽度自由排布。
- **词典结构化流式解析器 (`dictionary_parser.py`)**：
  - 专为大模型非确定性输出设计的渐进式流式状态机，能够实时从流式输出中提取各板块内容并实时注入卡片。

### 🔄 优化 (Optimized)
- 重写词典专用系统提示词（`default_prompts.py`），规范了标准输出协议。

---

## [1.3.0] - 2026-09-15

### ✨ 新增 (Added)
- **微软 Edge Neural TTS 高保真神经语音引擎 (`TTSManager`)**：
  - 深度集成微软 Edge 在线神经网络语音合成服务，提供极具自然度、呼吸感真人级别朗读；
  - 内置丰富多语种发音人（中文晓晓 `zh-CN-XiaoxiaoNeural`、日语七海 `ja-JP-NanamiNeural`、英语 `en-US-AriaNeural`、韩语 `ko-KR-SunHiNeural` 等）。
- **Windows 本地 SAPI5 离线降级与引导体系**：
  - 当无外网连接或网络受阻时，自动无缝降级调用本地 SAPI5 离线语音引擎；
  - 提供语音服务检测与 Windows 语音包安装指引对话框。
- **多媒体异步播放流水线**：
  - 基于 `PySide6.QtMultimedia` 实现音频数据内存缓冲与非阻塞异步播放，保障主界面 60FPS 丝滑流畅。

---

## [1.2.0] - 2026-09-15

### 🛡️ 架构重塑与安全性 (Security & Architecture)
- **自主研发防塌陷平衡分栏器 (`BalancedSplitter`)**：
  - 彻底攻克 Qt 原生 `QSplitter` 拖动至极限边缘导致某一侧面板永久折叠塌陷、无法复原的交互恶疾；
  - 左右面板强制设定 **280px 最小安全宽度**，有效抵御极端压迫拖拽；
  - **双击中线 1:1 对等复位**：双击中央分隔条手柄，两端面板瞬间等比例对齐恢复；
  - 提供醒目的悬停高亮动效与自定义拖动光标指示。

---

## [1.1.0] - 2026-09-14

### ✨ 新增 (Added)
- **逐句智能对齐与悬浮对照引擎 (`SentenceAligner`)**：
  - 针对中、英、日等多语种进行标点与语法感知的句子边界切分；
  - 鼠标悬停于原文句子时，对应目标译文句子自动触发青色同步高亮，助力精细化校对。
- **单句独立右键重译**：
  - 在输出面板中支持单句右键呼出独立重译菜单，仅针对有疑义的单句重新生成并原地热替换。
- **GLM-OCR 视觉图片识别与两阶段流水线 (`PipelineWorker`)**：
  - 支持快捷键 `Ctrl+V` 剪贴板截图直接粘贴与图片拖拽加载；
  - 接入 GLM-OCR / OpenAI Vision 视觉模型完成秒级高精文字识别，并自动将文本回填至输入框触发连贯翻译。

---

## [1.0.0] - 2026-09-13 (正式商用基线)

### 🚀 里程碑升级 (Milestone)
- **正式更名为 Just Translate**，确立专为高频深度语言处理打造的极简桌面生产力工具定位。
- **现代化视觉与主题体系**：
  - 实现暗黑极客 (Dark) 与明亮纯净 (Light) 双套高质感 QSS 样式表，支持跟随系统热切换；
  - 规范应用图标：采用 32 位完整 RGBA 透明通道，全面抠除黑底与杂色背景，确保多端系统托盘与任务栏无瑕融合。
- **本地 SQLite 历史记忆数据库 (`history_manager.py`)**：
  - 翻译、润色与查词历史自动本地持久化入库，无数据泄露风险；
  - 提供 `Ctrl+H` 历史检索面板，支持全文模糊搜索与一键回填。
- **网络鲁棒性与性能规范**：
  - 严格设定 30 秒硬超时限制，遭遇连接波动时自动触发指数退避重试，杜绝界面无限挂起死等。
- **低延迟精简模式 (Eco Mode)**：
  - 引入精简 Prompt 策略，在算力受限或低配置显卡下实现极速首字输出（TTFT）与 Token 节约。

---

## [0.1.0 - 0.9.0] - 2026-09-12 (原型孵化)

- 项目基础技术验证与原型搭建（早期代号 LingoDesk）。
- 基于 `PySide6` 完成桌面端窗口基础骨架。
- 研发兼容 OpenAI 规范的 SSE (Server-Sent Events) 流式 HTTP 客户端，实现逐字打字机效果。
- 初步打通翻译、润色与词典三大基础功能与提示词工程原型。
